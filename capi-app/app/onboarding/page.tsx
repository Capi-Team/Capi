// Initial Onboarding Flow — placeholder.

export default function OnboardingPage() {
  return (
    <main className="coffee-bg-muted min-h-screen p-6 text-[var(--coffee-ink)]">
      <p className="text-sm text-[var(--coffee-muted)]">Onboarding en construcción.</p>
    </main>
  );
}
