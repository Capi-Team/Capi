// Dynamic Learning Module Page — placeholder hasta definir contenido por módulo.

export default function LearningModulePage() {
  return (
    <main className="coffee-bg-muted min-h-screen p-6 text-[var(--coffee-ink)]">
      <p className="text-sm text-[var(--coffee-muted)]">Módulo en construcción.</p>
    </main>
  );
}
